<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_all_exercises_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Все упражнения:"
        android:textSize="20sp"
        android:layout_marginBottom="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_all_exercises"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"/>

    <Button
        android:id="@+id/btn_save_favorite"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сохранить как избранный план"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/btn_load_favorite"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Выбрать избранный план"
        android:layout_marginTop="8dp"/>

    <Button
        android:id="@+id/btn_schedule"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Запланировать на дату"
        android:layout_marginTop="8dp"/>

</LinearLayout>